# Local-only project notes — contains private contact info, must NOT be deployed/published
PROJECT_REFERENCE.md

# Local Claude Code config
.claude/

# OG image generation utility (local only)
gen_og_image.py
